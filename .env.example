# Copia este archivo a .env y ajusta los valores.
# El .env real NO se sube al repositorio.

# Puerto local del host donde se expone el contenedor (nginx hace proxy_pass aqui)
HOST_PORT=24750

# Ruta base bajo la que se sirve la app (debe terminar en /)
VITE_BASE=/franc/calculadora/
